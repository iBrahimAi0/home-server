# Home Server Dashboard - Frontend Configuration
# Copy this file to .env.local and point it at your backend on the LAN
# (or your Tailscale hostname/IP if accessing remotely).
#
# Find your server's LAN IP with: hostname -I

NEXT_PUBLIC_API_URL=http://SERVER-IP:3001
NEXT_PUBLIC_SOCKET_URL=http://SERVER-IP:3001
